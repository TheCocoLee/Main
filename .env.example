# Supabase → Project Settings → Database → Connection string → Transaction pooler.
# Use the POOLER host (aws-*.pooler.supabase.com), not db.<ref>.supabase.co:
# the pooler presents a publicly trusted certificate and survives serverless.
# Copy this file to .env.local and paste yours in. .env.local is gitignored.
DATABASE_URL=postgresql://postgres.kdmplsjjawcrmdiaflqy:YOUR-PASSWORD@aws-1-us-west-1.pooler.supabase.com:6543/postgres
